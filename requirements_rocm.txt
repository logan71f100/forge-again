# =============================================================================
# forge-again requirements (ROCM / AMD GPU)
# Install before requirements_versions.txt so the ROCm torch wheel satisfies
# `torch`/`torchvision` pins and pip doesn't pull CUDA wheels.
# =============================================================================

--extra-index-url https://download.pytorch.org/whl/rocm6.1

torch==2.13.0+rocm6.1
torchvision==0.18.0+rocm6.1