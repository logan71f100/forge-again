--extra-index-url https://download.pytorch.org/whl/cu126
setuptools>=70,<81   # was ==69.5.1; torch 2.13 needs >=70, but setuptools 81 removed pkg_resources which pytorch_lightning 1.9.4 still requires
GitPython==3.1.52   # security: clears 11 GHSAs (command-injection/path-traversal); API-compatible patch bump
Pillow>=11   # forced to 12.x by the modern stack (was 9.5.0)
accelerate==1.14.0
blendmodes>=2025   # numpy-2 / Pillow-12 compatible (2022 capped numpy<2, Pillow<10)
clean-fid==0.1.35
diskcache==5.6.3
einops==0.4.1
facexlib==0.3.0
# PORT TARGET: gradio 6 (current line) — full modernization, one migration.
# Chain: gradio 6 needs hub>=1.2 -> transformers must be 5.x (needs hub>=1.5).
fastapi>=0.115.2
gradio==6.20.0
httpcore
inflection==0.5.1
jsonmerge==1.8.0
kornia==0.6.7
lark==1.1.2
numpy>=2   # forced to 2.x by transformers5/diffusers/gradio6 (was 1.26.2)
omegaconf==2.2.3
open-clip-torch==2.20.0
piexif==1.1.3
protobuf==3.20.0
psutil>=5.9.5
pytorch_lightning==1.9.4
resize-right==0.0.2
safetensors>=0.4.2
scikit-image>=0.24   # numpy-2 compatible build (0.21 was numpy-1 ABI)
spandrel==0.3.4
spandrel-extra-arches==0.1.1
tomesd==0.1.3
# PINNED to the cu126 CUDA build — an unpinned torch resolves to a CPU wheel from PyPI
torch==2.13.0+cu126
torchvision==0.28.0+cu126
torchdiffeq==0.2.3
torchsde==0.2.6
transformers==5.14.1   # 5.x required: gradio 6 forces hub 1.x, which no transformers 4.x accepts
httpx>=0.26
pillow-avif-plugin==1.4.3
diffusers==0.39.0
# gradio_rangeslider has NO gradio-6 build — ControlNet Timestep Range gets
# paired gr.Sliders (or a 6-native component) during the ControlNet port stage
# gradio_rangeslider==0.0.8
gradio_imageslider==0.0.20
loadimg==0.1.2
tqdm>=4.66.1
peft==0.19.1
pydantic==2.8.2
huggingface-hub>=1.5,<2.0   # gradio 6 needs >=1.2, transformers 5 needs >=1.5
